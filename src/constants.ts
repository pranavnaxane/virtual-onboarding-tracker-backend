export enum HTTP_STATUS_CODE {
  CREATED = 201,
  BAD_REQUEST = 400,
  TOKEN_NOT_FOUND = 401,
  UNAUTHORISED = 403,
  NOT_FOUND = 404,
  SERVER_ERROR = 500,
}

export enum USER_TYPE {
  HR = "hr",
  USER = "user",
}
